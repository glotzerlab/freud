[build-system]
requires = ["setuptools", "wheel", "oldest-supported-numpy", "cython>=0.29.14", "scikit-build>=0.13.1", "cmake"]

[tool.black]
target-version = ['py36']

[tool.isort]
profile = 'black'
